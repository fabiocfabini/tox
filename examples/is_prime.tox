func is_prime(n: integer) -> integer {
    si n < 2 { return 0 }
    si n == 2 { return 1 }
    si n % 2 == 0 { return 0 }

    for(i: integer = 3; i < n; i = i + 1) {
        si n % i == 0 { return 0 }
    }

    return 1
}

func main() {
    N: integer = readi("How many numbers? (<0 si forever) ")

    print("Will check ", N, " numbers\n")
    si N >= 0 {
        for(i: integer; i < N; i = i + 1) {
            x: integer = readi("Enter a number: (0 to exit) ")
            si x == 0 { break }
            si(is_prime(x)) {
                print(x, " is prime\n")
            } aliter {
                print(x, " is not prime\n")
            }
        }
    } aliter {
        while(1) {
            x: integer = readi("Enter a number: (0 to exit) ")
            si x == 0 { break }
            si(is_prime(x)) {
                print(x, " is prime\n")
            } aliter {
                print(x, " is not prime\n")
            }
        }
    }

    print("Done\n")
}